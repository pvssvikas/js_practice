extends layout

block content
    h1= title

    h1  #{user}
    if results.length
        each user in results 
            h5|First Name----#{user.first_name}
              br
              |Last  Name----#{user.last_name}
              br
              |h5 user-id-------#{user._id} 
